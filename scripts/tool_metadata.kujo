# Reuse the native TOML parser for OS integration metadata.
values := args()
metadata := parse_toml(read_file(values[len(values) - 1]))
if len(values) > 1 && values[0] == "--version" {
    print("Kennel " + metadata["package"]["version"])
} else {
    print(to_json(metadata))
}
